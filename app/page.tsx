import { CharacterCard } from "@/components/characterCard";

export default function Home() {
  return (
    <div className="">
      {/* <CharacterCard character={character} onFavorite={() => {}} isFavorite={false} /> */}
    </div>
  );
}
